/**
 * Folio · /hoy
 *
 * Dashboard del día. Muestra agenda de turnos del profesional, KPIs y
 * acciones rápidas (walk-in). Es la primera ruta de validación visual
 * del MVP (es la pantalla que más se usa cada día).
 */

import { Dashboard } from "@/components/hoy/dashboard";

export default function HoyPage() {
  return <Dashboard />;
}
