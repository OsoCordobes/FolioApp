# Inventario de ramas y trabajo recuperable — 2026-09-08

Repositorio: `OsoCordobes/FolioApp`. Base revisada: `master` en
`2bfbe54137603e373a0fa2ab439d367dedf93415`, después de actualizar y depurar
referencias remotas. La revisión y la limpieza local se completaron. Esto no
acredita integración de los stashes ni despliegue de código.

## Ramas locales

Se encontraron **99 ramas locales**, incluidas `master` y la rama activa de
implementación `codex/market-ready`. Conservar ambas. Las otras **97** son
se retiraron después de verificar sus referencias y respaldarlas:

| Cantidad | Evidencia |
| --- | --- |
| 57 | Los 56 nombres `worktree-*` y `claude/vigorous-bartik-8ed0b7` son antecesores de `master`. Ninguno está asociado a un worktree existente. |
| 34 | El parche agregado de cada rama coincide exactamente, mediante `git patch-id --stable`, con el squash de su PR integrado: #84, #85, #87–94, #102–121 y #124–127. |
| 5 | Las ramas de fases 0–3 están incluidas en el PR #101, que reemplazó #95–99. |
| 1 | `feat/fase1-recordatorios-vercel-15min`: propuesta cerrada #82, conservada explícitamente como instrucciones en el workflow actual. |

Las cinco ramas de fases son `feat/fase0-premium-vendible`,
`feat/fase1-clinico`, `feat/fase2-especialidades-polish`,
`feat/fase3-portal-core` y `feat/fase3-portal-features`. Todas son antecesores
del head original de #101, `d5716375e03df14d9d176114e33bc00e73c71f72`.
Su árbol coincide exactamente con el squash integrado
`3429026f8b6c0513a54f7141e747c097bc25e979`:
`7abfca29fa65d583321db504fb6f22ae736bc1e3`.

La rama de cron contiene una sola modificación de frecuencia en `vercel.json`.
No integrarla como parte de la limpieza: el workflow
`.github/workflows/dispatch-recordatorios.yml` ya programa ejecuciones cada
15 minutos y conserva la instrucción para migrar a Vercel cuando corresponda.
El cierre de #82 documenta expresamente esa decisión. No se verificó el plan
actual de Vercel durante este inventario.

El manifiesto externo `branch-cleanup-candidates.json`, dentro del respaldo
local de recuperación creado para esta sesión, contiene las 97 referencias,
sus SHA esperados, la clasificación y evidencia. Antes de borrar cada referencia,
se comprobó que conservaba el SHA inventariado y que no estaba en uso.
El archivo externo `branches-before-cleanup.bundle` conserva todas las referencias
y ambos stashes; `git bundle verify` confirmó que el archivo es válido. La eliminación
de cada referencia se condicionó a su SHA esperado. Permanecen `master` y
`codex/market-ready`; los archivos y worktrees en uso se conservaron.

## PR abierto y rama remota

Solo permanece abierto [#159](https://github.com/OsoCordobes/FolioApp/pull/159),
`docs(audit): auditoría end-to-end post-incidente .env.local`, en
`claude/security-audit-key-rotation-6khvqf`, commit `d7254b3`.
Agrega un documento de 237 líneas y no cambia código. GitHub lo indicó como
integrable; las comprobaciones exitosas corresponden al 28 de agosto.
Sus observaciones históricas deben reconciliarse con la auditoría actual antes
de cerrar o reemplazar ese registro. Este inventario no cerró el PR ni escribió
comentarios externos.

## Stashes

Se conservaron ambos stashes. Ninguno contiene archivos `.env` entre sus rutas
inventariadas. No se leyeron secretos.

### `stash@{0}` — trabajo útil para adaptar

Mensaje: `On feat/audit-fixes: epitaxy: pre-switch from feat/audit-fixes`.
Se identificaron seis mejoras todavía ausentes de la base revisada:

| Ruta exacta | Comportamiento recuperable |
| --- | --- |
| `app/(portal)/portal/login/actions.ts` | Registrar código/estado del fallo de OTP sin revelar el email y manteniendo una respuesta uniforme para impedir enumeración. |
| `app/api/auth/callback/route.ts` | Registrar errores de la consulta de cuenta del portal para no confundir indisponibilidad con ausencia de cuenta. **Adaptar al RPC actual `paciente_cuenta_ensure`; el stash usa el antiguo `paciente_cuenta_actual`.** |
| `lib/db/errors.ts` | Traducir SQLSTATE `23514` a un error de validación comprensible. Revisar el mensaje específico de especialidad contra las restricciones actuales. |
| `lib/especialidades/psicologia/dashboard.tsx` | Capturar rechazos de la acción que carga escalas, finalizar el estado de carga y mostrar un error recuperable. |
| `lib/instrumentos/components/PlanillaRenderer.tsx` | Cambiar el aviso de cuestionario incompleto de `role="alert"` a `role="status"`, para no interrumpir al lector de pantalla en cada respuesta. |
| `lib/supabase/middleware.ts` | Detectar errores devueltos por RPC y búsqueda de membresía, además de excepciones de red. |

Portar selectivamente y verificar los comportamientos antes de descartar el
stash. Evitar copiar los logs de excepciones completas: preferir códigos y
mensajes estáticos sin datos personales ni contenido clínico. El parche completo
no se puede aplicar inversamente sobre la base actual; sus contextos han cambiado.

### `stash@{1}` — implementación anterior mayormente reemplazada

Mensaje: `On feat/fase0-premium-vendible: wip-fase0-agentes-interrumpidos`.
La base actual ya implementa modalidad de turnos, identidad prescriptora,
telemetría de descifrado y pruebas de esa telemetría. Los cambios del stash en
cardiología corresponden a una versión anterior e incompleta.

Además de seis archivos de código, contiene tres archivos entonces no rastreados:

- `supabase/migrations/20260704000072_M72_turno_modalidad_telemedicina.sql`
- `supabase/migrations/20260704000078_M78_prescriptor_droga_referencia.sql`
- `tests/unit/crypto-decrypt-telemetry.test.ts`

**No aplicar este stash completo.** Sus migraciones preceden las versiones
canónicas actuales y su código prescriptor usa nombres de columnas distintos
(`refeps`/`jurisdiccion`, frente a `registro_prescriptor`/`jurisdiccion_matricula`).
Las pruebas útiles de telemetría ya están en `tests/unit/crypto-roundtrip.test.ts`.
Conservarlo en el respaldo como evidencia de trabajo anterior; no se recomienda
recuperación directa durante esta implementación.

## Cierre de los stashes

Se recuperaron selectivamente los seis aportes útiles del primer stash: diagnóstico
sanitizado de OTP, callback y middleware; SQLSTATE 23514; error/reintento del
historial de instrumentos y aviso no interruptivo de cuestionario incompleto.
Sus pruebas focales pasaron. No se aplicaron migraciones obsoletas del segundo stash.

Antes de retirar ambos stashes se creó `stashes-before-cleanup.bundle` en la carpeta
privada de recuperación, con referencias explícitas a **ambos** commits. Se restauró
en un repositorio independiente y se comprobó también el tercer padre de cada stash
(archivos entonces no rastreados). Esto evita depender de que `git bundle --all`
incluya entradas antiguas del reflog de stash. El bundle de ramas se conserva además.

La PR 159 fue revisada por separado: ver
[reconciliación](2026-09-08-pr159-reconciliacion.md). No contiene funcionalidad que
deba mezclarse con la aplicación actual.
